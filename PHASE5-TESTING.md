# دليل المرحلة 5️⃣ - اختبار لوحة تحكم الأدمن

⚠️ استبدل ملفات المشروع بالملفات الجديدة، ثم:
```
npm install
npm run dev
```

---

## 📁 الملفات الجديدة

```
src/
├── components/
│   ├── ImageUploader.tsx  ← رفع صورة واحدة (يُستخدم داخل النماذج)
│   └── ProductForm.tsx    ← نموذج إضافة/تعديل منتج كامل
│
└── app/
    ├── account/page.tsx           ← صفحة "حسابي" + سجل الطلبات + رابط سريع للوحة التحكم (للأدمن)
    ├── wishlist/page.tsx          ← قائمة الأمنيات (جديدة - كانت مفقودة)
    ├── api/wishlist/route.ts      ← API الأمنيات (جديد - كان مفقوداً)
    │
    └── admin/
        ├── layout.tsx              ← حماية اللوحة (أدمن فقط) + القائمة الجانبية
        ├── page.tsx                ← الإحصائيات (بطاقات + رسم بياني + تنبيهات مخزون)
        ├── categories/page.tsx     ← جدول الأقسام + نموذج إضافة/تعديل مع رفع صورة
        ├── coupons/page.tsx        ← جدول الكوبونات + نموذج إضافة
        ├── orders/page.tsx         ← جدول الطلبات + تحديث الحالة مباشرة من القائمة
        └── products/
            ├── page.tsx            ← جدول المنتجات
            ├── new/page.tsx        ← إضافة منتج جديد
            └── [id]/edit/page.tsx  ← تعديل منتج موجود
```

---

## ✅ اختبار كامل (رحلة أدمن حقيقية من الصفر)

### 1. سجّل دخول كأدمن
اذهب لـ `/login`، سجّل دخول بـ `admin@leadybag.com` / `password123`

### 2. افتح لوحة التحكم
اذهب لـ `/account` واضغط زر "لوحة التحكم"، أو مباشرة `/admin`

**يجب أن ترى**: بطاقات إحصائيات (كلها أصفار حالياً)، ورسم بياني فارغ، وقائمة جانبية (الإحصائيات، الأقسام، المنتجات، الطلبات، الكوبونات).

### 3. أضف قسماً من الواجهة (بدون Postman هذه المرة!)
اذهب لـ `/admin/categories` → اضغط "قسم جديد" → املأ الاسم بالعربية والإنجليزية والـ slug → جرّب رفع صورة حقيقية (يتطلب إعداد Cloudinary من المرحلة 2) → احفظ.

**يجب أن ترى**: القسم يظهر في الجدول فوراً بصورته.

### 4. أضف منتجاً
اذهب لـ `/admin/products` → "منتج جديد" → املأ كل الحقول (الاسم، الوصف، السعر، اختر القسم الذي أضفته، ارفع صورة أو أكثر) → احفظ.

**يجب أن ترى**: تحويل تلقائي لجدول المنتجات، والمنتج الجديد ظاهر فيه.

### 5. تحقق أن المنتج ظهر في الموقع الفعلي
اذهب للصفحة الرئيسية `/` أو `/products` (كمستخدم عادي وليس في لوحة التحكم).

**يجب أن ترى**: نفس المنتج بصورته الحقيقية وسعره.

### 6. جرّب تعديل وحذف
عد لـ `/admin/products`، اضغط أيقونة التعديل ✏️ على المنتج، غيّر السعر، احفظ. تأكد أن السعر تغيّر في الموقع.

### 7. أضف كوبوناً من الواجهة
اذهب لـ `/admin/coupons` → أضف كوبوناً (كود + نسبة خصم + تواريخ).

### 8. قم بعملية شراء كاملة (كعميل) ثم راجعها كأدمن
1. سجّل خروج، سجّل دخول أو أنشئ حساب عميل عادي
2. أضف المنتج للسلة، طبّق الكوبون، أكمل الشراء (دفع عند الاستلام)
3. سجّل دخول مجدداً كأدمن، اذهب لـ `/admin/orders`
4. **يجب أن ترى**: الطلب الجديد في الجدول - جرّب تغيير حالته من القائمة المنسدلة إلى "تم الشحن"
5. اذهب لـ `/admin` (الإحصائيات) - **يجب أن ترى** الأرقام تحدّثت (عدد الطلبات، المنتجات)

### 9. جرّب قائمة الأمنيات
كعميل، من صفحة تفاصيل أي منتج، اضغط أيقونة القلب ❤️، ثم اذهب لـ `/wishlist` للتأكد من ظهوره، وجرّب زر "أضف للسلة" لنقله.

---

## 🎯 ماذا بعد؟
بعد نجاح هذا الاختبار، سيكون المتجر **يعمل بوظائفه الكاملة تقريباً من الأدمن حتى العميل**. الخطوة التالية: **المرحلة 6: الأمان والحماية** (فحص شامل: محاولات دخول غير مصرح، حقن بيانات خبيثة، HTTPS، 2FA للأدمن) ثم **المرحلة 7: التحسين النهائي والنشر** (رفع الموقع فعلياً على الإنترنت بدومين حقيقي).

## ❌ إذا واجهت خطأ
انسخ لي: اسم الصفحة + رسالة الخطأ كاملة (لقطة شاشة أفضل).
