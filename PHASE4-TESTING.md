# دليل المرحلة 4️⃣ - اختبار الواجهة الأمامية

⚠️ استبدل ملفات المشروع بالملفات الجديدة، ثم:
```
npm install
npm run dev
```

---

## 📁 الملفات الجديدة

```
src/
├── lib/
│   ├── i18n.ts          ← كل نصوص الواجهة (عربي/إنجليزي)
│   └── apiClient.ts     ← دالة موحدة للاتصال بالـ API (تضيف التوكن تلقائياً)
│
├── context/
│   └── AppContext.tsx   ← يدير: اللغة، اتجاه الصفحة RTL/LTR، حالة تسجيل الدخول، عدد عناصر السلة
│
├── components/
│   ├── Header.tsx       ← الهيدر (شعار، بحث، تبديل لغة، أمنيات، سلة، حساب)
│   ├── Footer.tsx       ← الفوتر
│   └── ProductCard.tsx  ← بطاقة عرض منتج (تُستخدم في عدة صفحات)
│
└── app/
    ├── layout.tsx (محدّث) ← يضم Header + Footer + AppProvider
    ├── page.tsx (محدّث)   ← الصفحة الرئيسية (بانر + أقسام + منتجات مميزة)
    ├── products/
    │   ├── page.tsx        ← صفحة المنتجات (فلاتر + بحث + ترقيم صفحات)
    │   └── [id]/page.tsx   ← تفاصيل منتج (صور + تقييمات + توصيات)
    ├── cart/page.tsx       ← السلة (تحديث كمية، حذف، كوبون)
    ├── checkout/page.tsx  ← الدفع (عنوان شحن + طريقة دفع)
    ├── order-success/page.tsx ← تأكيد الطلب
    ├── login/page.tsx
    └── register/page.tsx
```

---

## ✅ اختبار كامل خطوة بخطوة (بدون Postman هذه المرة - كل شيء بالمتصفح)

### 1. افتح الصفحة الرئيسية
```
http://localhost:3000
```
**يجب أن ترى**: بانر وردي بعنوان "كل ما تحتاجه المرأة السودانية"، وقسم "لا توجد أقسام بعد" (لأن قاعدة البيانات فارغة - طبيعي).

### 2. جرّب تبديل اللغة
اضغط على زر "EN" أعلى يمين الصفحة (في الهيدر).
**يجب أن ترى**: كل النصوص تتحول للإنجليزية، واتجاه الصفحة يتحول من اليمين-لليسار إلى اليسار-لليمين تلقائياً. اضغط عليه مرة أخرى للعودة للعربية.

### 3. أنشئ حساباً جديداً
اذهب إلى `/register`، أنشئ حساب عميل عادي (اسم + بريد + كلمة مرور).
**يجب أن ترى**: تحويل تلقائي للصفحة الرئيسية، وظهور أيقونة "حسابي" بدلاً من زر تسجيل الدخول في الهيدر.

### 4. سجّل خروج ثم دخول كأدمن
اضغط أيقونة الخروج، ثم اذهب لـ `/login` وسجّل دخول بحساب الأدمن (`admin@leadybag.com` / `password123`).

### 5. أضف قسماً ومنتجاً (عبر Postman كما في المرحلة 2، لأن لوحة تحكم الأدمن الرسومية ستُبنى في المرحلة 5)
استخدم توكن الأدمن (من `/login`) لإضافة قسم ومنتج كما فعلنا في اختبارات المرحلة 2.

### 6. أعد فتح الصفحة الرئيسية و `/products`
**يجب أن ترى**: القسم والمنتج اللذين أضفتهما يظهران الآن في الموقع فعلياً بالصور والأسعار.

### 7. جرّب رحلة شراء كاملة
1. من `/products`، اضغط على منتج لفتح تفاصيله
2. جرّب الفلاتر: نطاق السعر، الترتيب حسب السعر
3. أضف تقييماً (نجوم + تعليق) على المنتج
4. اضغط "أضف للسلة"
5. اذهب لـ `/cart`، جرّب تغيير الكمية، وتطبيق كوبون `WELCOME10` (إن أنشأته في المرحلة 2)
6. اضغط "إتمام الشراء"، اختر "الدفع عند الاستلام"، اكتب عنوان شحن، وأكّد الطلب
7. **يجب أن ترى**: صفحة "تم استلام طلبك بنجاح" مع رقم تتبع

---

## 🎯 ماذا بعد؟
بعد نجاح هذه الاختبارات، ننتقل لـ **المرحلة 5: لوحة تحكم الأدمن الرسومية** (بدلاً من استخدام Postman لإضافة المنتجات، ستكون لديك واجهة كاملة برفع صور، جداول، وإحصائيات).

## ❌ إذا واجهت خطأ
انسخ لي رسالة الخطأ كاملة (سواء ظهرت في المتصفح أو في الـ Terminal) واسم الصفحة التي كنت فيها.
