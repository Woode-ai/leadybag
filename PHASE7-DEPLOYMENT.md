# دليل المرحلة 7️⃣ - النشر الفعلي على الإنترنت

⚠️ **قبل البدء**: استبدل ملفات المشروع بالملفات الجديدة (احتفظ بـ `.env.local`)، ثم:
```
npm install
```
هذا التحديث يضيف **نظام التخزين المؤقت Redis** (الميزة #14 من متطلباتك الأصلية - كانت المكتبة مثبتة لكن غير مستخدمة فعلياً، فأصلحت هذا) على المنتجات والأقسام، ويصلح مشكلتين كانتا ستمنعان نجاح النشر (اشرحهما أدناه).

---

## 🔴 نقطة صراحة مهمة جداً قبل أي شيء: لماذا ليس Vercel؟

طلبك الأصلي ذكر "Vercel أو AWS". لكن مشروعك يحتوي **دردشة مباشرة (Socket.io)** تعمل عبر `server.js` مخصص - وهذا يتطلب سيرفراً "دائم التشغيل" (Persistent Server).

**Vercel لا يدعم هذا إطلاقاً** - هي منصة "Serverless" بمعنى أن كل طلب يُشغَّل في بيئة مؤقتة تُغلق بعد الرد مباشرة، ولا يمكنها الاحتفاظ باتصال WebSocket مفتوح للدردشة. لو نشرت المشروع على Vercel كما هو، الموقع سيعمل لكن **الدردشة المباشرة لن تعمل أبداً**.

الخياران الصادقان أمامك:
1. **استخدام منصة تدعم سيرفر دائم** (Railway أو Render) - نفس بساطة Vercel تقريباً (ربط GitHub وحفظ)، وهذا ما سنستخدمه في هذا الدليل. ✅ موصى به
2. **حذف ميزة الدردشة المباشرة** والعودة لـ Next.js القياسي لتتمكن من النشر على Vercel

سنسلك الطريق الأول (Railway) لأنه يحافظ على كل الميزات الـ 15 كاملة كما طلبت أصلاً.

---

## 📁 الملفات الجديدة/المعدّلة

```
src/lib/redis.ts          ← نظام التخزين المؤقت (جديد - كان مفقوداً رغم وجوده في قائمة المتطلبات)
src/app/api/products/route.ts (محدّث)      ← كاش لمدة 60 ثانية + إلغاؤه عند الإضافة
src/app/api/products/[id]/route.ts (محدّث) ← إلغاء الكاش عند التعديل/الحذف
src/app/api/categories/route.ts (محدّث)    ← كاش لمدة 5 دقائق + إلغاؤه عند الإضافة
src/app/api/categories/[id]/route.ts (محدّث) ← إلغاء الكاش عند التعديل/الحذف
server.js (محدّث)         ← إصلاح حرج: كان مُثبَّتاً على localhost:3000 فقط، وهذا يمنع أي منصة استضافة
                             من الوصول له. الآن يقرأ PORT و 0.0.0.0 تلقائياً من بيئة الإنتاج
.gitignore (جديد)         ← كان مفقوداً - ضروري لمنع رفع node_modules و.env.local على GitHub
```

---

## 🚀 خطوات النشر الكاملة

### الخطوة 1: جهّز قاعدة بيانات إنتاجية (MongoDB Atlas)
إن كنت تستخدم MongoDB محلياً على جهازك، لن يراه الإنترنت. أنشئ قاعدة سحابية:
1. اذهب لـ https://www.mongodb.com/cloud/atlas وأنشئ حساباً مجانياً (إن لم يكن لديك بالفعل)
2. أنشئ Cluster مجاني (M0)
3. من `Database Access`: أنشئ مستخدم قاعدة بيانات (اسم مستخدم + كلمة مرور قوية)
4. من `Network Access`: اضغط "Allow Access from Anywhere" (0.0.0.0/0) - ضروري لأن Railway يستخدم عناوين IP متغيرة
5. من `Connect → Drivers`: انسخ رابط الاتصال، شكله:
   ```
   mongodb+srv://username:password@cluster.mongodb.net/leadybag
   ```

### الخطوة 2: جهّز Redis إنتاجي (Upstash - مجاني)
Redis المحلي (`127.0.0.1`) لن يعمل بعد النشر أيضاً:
1. اذهب لـ https://upstash.com وأنشئ حساباً مجانياً
2. أنشئ قاعدة Redis جديدة (اختر أقرب منطقة جغرافية لك)
3. من لوحة التحكم، انسخ رابط الاتصال (`Redis URL` بصيغة `rediss://...`)

### الخطوة 3: ارفع الكود على GitHub
```
cd leadybag
git init
git add .
git commit -m "leadybag - جاهز للنشر"
```
أنشئ مستودعاً جديداً فارغاً على https://github.com/new (لا تضف README أو .gitignore من GitHub نفسه)، ثم:
```
git remote add origin https://github.com/اسم_المستخدم/leadybag.git
git branch -M main
git push -u origin main
```

### الخطوة 4: انشر على Railway
1. اذهب لـ https://railway.app وسجّل دخول بحساب GitHub
2. اضغط "New Project" → "Deploy from GitHub repo" → اختر مستودع `leadybag`
3. Railway سيكتشف تلقائياً أنه مشروع Node.js وسيشغّل `npm install` ثم `npm run build` ثم `npm run start`
4. من تبويب "Variables"، أضف كل متغيرات البيئة (انسخها من `.env.local` عندك، لكن **حدّث القيم التالية**):
   ```
   MONGODB_URI=<رابط Atlas من الخطوة 1>
   REDIS_URL=<رابط Upstash من الخطوة 2>
   JWT_SECRET=<نفس القيمة أو أنشئ جديدة أطول>
   NEXTAUTH_URL=<سيُحدَّد في الخطوة التالية بعد معرفة الدومين>
   CLOUDINARY_CLOUD_NAME=...
   CLOUDINARY_API_KEY=...
   CLOUDINARY_API_SECRET=...
   STRIPE_SECRET_KEY=...
   STRIPE_WEBHOOK_SECRET=<سيُحدَّد في الخطوة 7 بعد ربط الـ Webhook>
   PAYMOB_API_KEY=...
   PAYMOB_INTEGRATION_ID=...
   PAYMOB_IFRAME_ID=...
   ADMIN_EMAIL=admin@leadybag.com
   ADMIN_PASSWORD=<غيّرها لكلمة مرور قوية حقيقية - لا تستخدم password123 في الإنتاج!>

   # إعدادات SMTP لإرسال بريد تفعيل الحساب وتأكيد الطلبات (بدونها، الموقع يعمل لكن لا تُرسَل الإيميلات فعلياً)
   SMTP_HOST=smtp-relay.brevo.com
   SMTP_PORT=587
   SMTP_USER=<من لوحة تحكم Brevo أو أي خدمة SMTP اخترتها>
   SMTP_PASS=<من نفس اللوحة>
   EMAIL_FROM="leadybag <no-reply@leadybag.com>"
   ```
   **من أين تحصل على SMTP مجاناً؟** أسهل خيار: https://www.brevo.com (يعطي 300 بريد/يوم مجاناً بدون بطاقة ائتمان) → من "SMTP & API" في لوحة التحكم، انسخ Host/Port/Login/Password مباشرة. البديل: Gmail (`SMTP_HOST=smtp.gmail.com`, `SMTP_PORT=587`) مع "App Password" تُنشئه من إعدادات أمان حساب Google (وليس كلمة مرور Gmail العادية).
5. اضغط "Deploy" وانتظر انتهاء البناء (يظهر السجل مباشرة، راقبه لأي أخطاء)

### الخطوة 5: اربط دومين حقيقي + SSL
1. من إعدادات المشروع في Railway → "Settings" → "Networking" → "Custom Domain"
2. أدخل الدومين الذي اشتريته (مثلاً `leadybag.com`)
3. Railway سيعطيك سجل DNS (CNAME) يجب إضافته عند شركة الدومين (Namecheap، GoDaddy، إلخ)
4. **شهادة SSL/HTTPS تُصدَر تلقائياً ومجاناً** (Let's Encrypt) خلال دقائق من إضافة الدومين - لا حاجة لأي إعداد يدوي
5. عد لمتغيرات البيئة وحدّث:
   ```
   NEXTAUTH_URL=https://leadybag.com
   ALLOWED_ORIGIN=https://leadybag.com
   ```

### الخطوة 6: أنشئ حساب الأدمن على قاعدة البيانات الإنتاجية
من جهازك محلياً، مؤقتاً، غيّر `MONGODB_URI` في `.env.local` ليشير لرابط Atlas الإنتاجي، ثم:
```
npm run create-admin
```
هذا ينشئ حساب الأدمن في قاعدة البيانات الحقيقية (وليس المحلية). بعدها أعد `.env.local` لقيمته المحلية إن أردت الاستمرار بالتطوير محلياً.

### الخطوة 7: فعّل Webhook الدفع الحقيقي (Stripe)
1. من https://dashboard.stripe.com/webhooks → "Add endpoint"
2. الرابط: `https://leadybag.com/api/payment/webhook`
3. اختر الحدث: `checkout.session.completed`
4. انسخ "Signing secret" الذي يظهر، وأضفه في متغيرات Railway:
   ```
   STRIPE_WEBHOOK_SECRET=whsec_...
   ```

---

## ✅ اختبار نهائي شامل (على الموقع الحقيقي المنشور)

1. افتح `https://leadybag.com` - تأكد أن الصفحة تعمل وتظهر بـ HTTPS (قفل أخضر في المتصفح)
2. سجّل دخول كأدمن، فعّل 2FA (إن لم تكن فعّلته)
3. أضف **3 أقسام حقيقية** بصور فعلية
4. أضف **10 منتجات حقيقية** بأسعار وصور ووصف حقيقي لكل واحد
5. أنشئ كوبون خصم حقيقي
6. من متصفح آخر (أو وضع تصفح خفي)، أنشئ حساب عميل جديد، تصفح المنتجات، طبّق الكوبون، أكمل عملية شراء كاملة (جرّب "الدفع عند الاستلام" أولاً لأنه الأبسط)
7. عد لحساب الأدمن، تحقق من ظهور الطلب في `/admin/orders`، وتحديث حالته
8. تحقق من لوحة `/admin` أن الإحصائيات تحدّثت
9. جرّب الدردشة المباشرة بين نافذتين (بنفس أسلوب اختبار المرحلة 2)
10. تحقق من سرعة تحميل صفحة `/products` عند تحديثها عدة مرات متتالية خلال أقل من دقيقة - يجب أن تكون شبه فورية (هذا أثر الكاش)

---

## 🎉 الخلاصة
بهذا يكون متجر **leadybag** مكتملاً وجاهزاً فعلياً للعمل: 15 ميزة متقدمة، أمان شامل، ونشر حقيقي بدومين وSSL. أي تحسينات إضافية من هنا (خطة موبايل React Native، تحسينات SEO أعمق، إلخ) يمكننا تناولها كخطوات منفصلة حسب حاجتك.

## ❌ إذا واجهت خطأ في أي خطوة
انسخ لي: رقم الخطوة + رسالة الخطأ كاملة (من سجلات Railway "Deployments → Logs" إن كان خطأ نشر، أو من المتصفح إن كان خطأ في الموقع نفسه).
