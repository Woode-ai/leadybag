# leadybag - ملف الاختبارات الشامل الموحّد

هذا الملف يجمع اختبار **كل ميزة في المشروع** في مكان واحد، بالترتيب المنطقي الصحيح من الإعداد الأول حتى النشر. نفّذه بالترتيب من الأعلى للأسفل.

---

## 0️⃣ الإعداد قبل أي اختبار

### تثبيت المشروع
```
npm install
```

### تعبئة ملف `.env.local`
انسخ `.env.local.example` إلى `.env.local` واملأ **على الأقل** هذه القيم الإلزامية لتشغيل الموقع أساساً:
```
MONGODB_URI=mongodb://127.0.0.1:27017/leadybag
JWT_SECRET=أي-نص-عشوائي-طويل-وسري
NEXTAUTH_URL=http://localhost:3000
ADMIN_EMAIL=admin@leadybag.com
ADMIN_PASSWORD=password123
```

القيم التالية **اختيارية** لتشغيل الموقع أساساً، لكنها **إلزامية** لاختبار الميزات المرتبطة بها تحديداً - إن تركتها فارغة، تلك الميزة فقط لن تعمل بينما باقي الموقع يعمل بشكل طبيعي:

| المتغير | الميزة التي يفعّلها | إن تُرك فارغاً |
|---|---|---|
| `CLOUDINARY_*` | رفع صور المنتجات/الأقسام | ستظهر رسالة خطأ واضحة عند محاولة الرفع |
| `REDIS_URL` | التخزين المؤقت (كاش) | الموقع يعمل بنفس الشكل تماماً، فقط بدون تسريع الكاش |
| `SMTP_*` | بريد تفعيل الحساب + تأكيد الطلب | الحساب يُنشأ بنجاح لكن لا يصل بريد فعلي (يظهر تنبيه في الـ Terminal فقط) |
| `STRIPE_SECRET_KEY` | الدفع بالبطاقة | خيار "الدفع عند الاستلام" يبقى يعمل بشكل طبيعي |
| `PAYMOB_*` | الدفع عبر PayMob | نفس ما سبق |

### إنشاء حساب الأدمن
```
npm run create-admin
```

### تشغيل المشروع
```
npm run dev
```
يجب أن تظهر رسالة "💬 نظام الدردشة المباشرة (Socket.io) جاهز" في الـ Terminal.

---

## 1️⃣ اختبار الصحة الأساسي

افتح `http://localhost:3000/api/health` — يجب أن ترى `"status": "success"` وقائمة كل النماذج السبعة (User, Category, Product, Order, Cart, Coupon, Analytics).

---

## 2️⃣ التسجيل، تسجيل الدخول، وتفعيل البريد الإلكتروني

### أ) التسجيل
1. اذهب لـ `/register`، أنشئ حساب عميل جديد ببريدك الإلكتروني الحقيقي (إن كنت أعددت SMTP) أو أي بريد وهمي (إن لم تُعدَّه بعد)
2. **يجب أن ترى**: تحويل تلقائي للصفحة الرئيسية، وتسجيل دخول فوري

### ب) تفعيل البريد (إن كان SMTP مُعداً)
1. تحقق من بريدك الوارد - يجب أن تصلك رسالة "فعّل حسابك في leadybag" خلال ثوانٍ
2. اضغط زر "تفعيل الحساب" داخل الرسالة
3. **يجب أن ترى**: صفحة `/verify-email` تعرض "تم تفعيل بريدك بنجاح"
4. اذهب لـ `/account` — يجب ألا يظهر تنبيه "لم تُفعِّل بريدك بعد" (لأنه اختفى تلقائياً بعد التفعيل بدون إعادة تسجيل دخول)

### ج) إن لم يكن SMTP مُعداً
1. اذهب لـ `/account` مباشرة بعد التسجيل
2. **يجب أن ترى**: تنبيهاً أصفر "لم تُفعِّل بريدك الإلكتروني بعد" مع زر "إعادة إرسال رابط التفعيل"
3. تحقق من الـ Terminal - يجب أن ترى تنبيهاً: `⚠️ لم يُرسَل بريد إلى ... لأن إعدادات SMTP غير مُعبّأة`
4. هذا يثبت أن الحساب يعمل بشكل طبيعي حتى بدون بريد فعلي - فقط التذكير موجود

### د) اختبار إعادة الإرسال
اضغط "إعادة إرسال رابط التفعيل" 6 مرات متتالية بسرعة.
**يجب أن ترى**: بعد عدة محاولات، رسالة "محاولات كثيرة، حاول مرة أخرى بعد X دقيقة" - هذا يثبت أن تحديد معدل الطلبات يحمي من إساءة الاستخدام.

### هـ) تسجيل دخول الأدمن
سجّل خروج، ثم سجّل دخول بـ `admin@leadybag.com` / `password123`.

---

## 3️⃣ إدارة المتجر من لوحة تحكم الأدمن

### أ) الحماية
سجّل خروج، حاول فتح `/admin` مباشرة → يجب أن يُعاد توجيهك لـ `/login`.
سجّل دخول بحساب عميل عادي (وليس أدمن)، حاول فتح `/admin` → يجب أن يُعاد توجيهك للصفحة الرئيسية.

### ب) الأقسام
سجّل دخول كأدمن → `/admin/categories` → أضف قسماً (اسم عربي/إنجليزي + slug + صورة) → تحقق من ظهوره في الجدول.

**اختبار رفع الصور**: إذا لم يظهر `CLOUDINARY_*` في `.env.local`، يجب أن تظهر رسالة خطأ واضحة: "رفع الصور غير مُفعَّل بعد - أضف CLOUDINARY_CLOUD_NAME..." بدل خطأ تقني مبهم. إن أضفت بيانات Cloudinary حقيقية وأعدت تشغيل السيرفر، يجب أن ترفع الصورة بنجاح ويظهر رابطها.

### ج) المنتجات
`/admin/products` → أضف منتجاً كاملاً (اسم، وصف، سعر، قسم، مخزون، ألوان، مقاسات، صور) → عدّله → تحقق من ظهوره وتحديثه في `/products` الفعلية.

### د) الطلبات والكوبونات
أنشئ كوبوناً من `/admin/coupons`. سنستخدمه في اختبار الشراء أدناه. راقب `/admin/orders` بعد إتمام أي عملية شراء لتحديث حالتها.

### هـ) لوحة الإحصائيات
`/admin` — تحقق من ظهور الإيرادات، عدد الطلبات، رسم بياني 7 أيام، أكثر المنتجات مبيعاً، وتنبيهات المخزون المنخفض.

---

## 4️⃣ رحلة شراء كاملة (من زاوية العميل)

1. من `/products`، جرّب الفلاتر (السعر، الترتيب) والبحث
2. افتح منتجاً، أضف تقييماً بالنجوم وتعليقاً
3. أضفه للسلة، اذهب لـ `/cart`، جرّب تغيير الكمية والحذف
4. طبّق كود الكوبون الذي أنشأته
5. أكمل الشراء بـ "الدفع عند الاستلام"
6. **يجب أن ترى**: صفحة تأكيد الطلب مع رقم التتبع
7. **إن كان SMTP مُعداً**: تحقق من وصول بريد "تم استلام طلبك" لصندوقك خلال ثوانٍ
8. كأدمن، تحقق من ظهور الطلب في `/admin/orders` وحدّث حالته إلى "تم الشحن"
9. كعميل، تحقق من `/account` أن الحالة الجديدة ظهرت في "طلباتي"

---

## 5️⃣ الدردشة المباشرة (الميزة التي لم يكن لها مكان ظاهر - الآن مكانها محدد بدقة)

### مكان الدردشة بالضبط
- **للعميل**: فقاعة دائرية عائمة أسفل يمين أي صفحة في الموقع (تظهر فقط إذا كنت مسجلاً دخولك كعميل)
- **للأدمن**: صفحة كاملة في `/admin/chats` (رابط "الدردشات المباشرة" في القائمة الجانبية)

### خطوات الاختبار
1. افتح نافذتي متصفح مختلفتين (أو نافذة عادية + نافذة تصفح خفي)
2. في الأولى: سجّل دخول كعميل عادي، اضغط فقاعة الدردشة أسفل يمين الصفحة، اكتب رسالة وأرسلها
3. في الثانية: سجّل دخول كأدمن، افتح `/admin/chats`
4. **يجب أن ترى**: اسم العميل يظهر في قائمة المحادثات فوراً مع رسالته
5. اضغط على المحادثة، اكتب رداً
6. **يجب أن ترى**: الرد يصل للعميل في النافذة الأولى **فوراً بدون تحديث الصفحة** (هذا هو البث اللحظي عبر Socket.io)
7. أغلق فقاعة الدردشة وأعد فتحها - يجب أن يظهر سجل المحادثة كاملاً (محفوظ في قاعدة البيانات، وليس مؤقتاً فقط)

---

## 6️⃣ الأمان

### أ) تحديد معدل محاولات الدخول
جرّب تسجيل الدخول بكلمة مرور خاطئة 6 مرات متتالية لنفس الحساب.
**يجب أن ترى**: بعد 5 محاولات، رسالة "الحساب مقفل مؤقتاً - حاول بعد 15 دقيقة".

### ب) الحماية من حقن NoSQL
في نموذج تسجيل الدخول، جرّب إرسال قيمة غريبة في البريد الإلكتروني (عبر Postman) مثل `{"$gt": ""}` بدل نص عادي.
**يجب أن يُرفض** الطلب مباشرة برسالة "بيانات غير صحيحة" (بفضل Zod + طبقة التنظيف الإضافية).

### ج) المصادقة الثنائية (2FA)
سجّل دخول كأدمن → `/admin/security` → فعّل 2FA (امسح رمز QR بتطبيق Google Authenticator أو Authy) → سجّل خروج → سجّل دخول من جديد.
**يجب أن يُطلب منك** كود مكوّن من 6 أرقام من التطبيق قبل إكمال الدخول.

### د) هيدرز الأمان
افتح أدوات المطور في المتصفح (F12) → تبويب Network → افتح أي صفحة → تحقق من وجود هيدرز: `X-Frame-Options`, `X-Content-Type-Options`, `Strict-Transport-Security`.

---

## 7️⃣ التخزين المؤقت (Redis Cache)

إن أعددت `REDIS_URL`:
1. افتح `/products` عدة مرات متتالية خلال أقل من دقيقة - لاحظ السرعة
2. من الـ Terminal، راقب أن طلبات MongoDB لا تتكرر لكل تحديث (لأن النتيجة تُقرأ من Redis مباشرة لمدة 60 ثانية)
3. أضف منتجاً جديداً من `/admin/products` ثم ارجع لـ `/products` فوراً
4. **يجب أن يظهر** المنتج الجديد **فوراً** (لأن إضافته ألغت الكاش القديم تلقائياً) وليس بعد انتظار 60 ثانية

---

## 8️⃣ الدفع الإلكتروني (Stripe / PayMob)

راجع تفاصيل هذا الاختبار في `PHASE2-PART2-TESTING.md` (يتطلب حسابات تجريبية مجانية من Stripe/PayMob).

---

## 9️⃣ خريطة الملفات الكاملة (مرجع سريع)

| الميزة | أين تجدها |
|---|---|
| تسجيل/دخول/تفعيل بريد | `/register`, `/login`, `/verify-email`, `src/app/api/auth/*` |
| الصفحة الرئيسية والمنتجات | `/`, `/products`, `/products/[id]` |
| السلة والدفع | `/cart`, `/checkout`, `/order-success` |
| حسابي والطلبات | `/account` |
| قائمة الأمنيات | `/wishlist` |
| **الدردشة (عميل)** | فقاعة عائمة في كل صفحة (`src/components/ChatWidget.tsx`) |
| **الدردشة (أدمن)** | `/admin/chats` |
| لوحة تحكم الأدمن | `/admin`, `/admin/products`, `/admin/categories`, `/admin/orders`, `/admin/coupons` |
| الأمان و2FA | `/admin/security` |
| كل واجهات API الخلفية | `src/app/api/**` (راجع `ALL-CODE-MASTER.md` للقائمة الكاملة) |

---

## 🔟 النشر

راجع `PHASE7-DEPLOYMENT.md` للخطوات الكاملة (MongoDB Atlas، Upstash Redis، Brevo SMTP، Railway، دومين حقيقي + SSL تلقائي، Stripe Webhook). يتضمن الآن أيضاً كل متغيرات SMTP اللازمة لتفعيل البريد الإلكتروني على الموقع الحقيقي بعد النشر.

---

## ❌ إذا واجهت أي خطأ في أي اختبار أعلاه
انسخ لي: رقم القسم (مثلاً "5️⃣ الدردشة") + رسالة الخطأ كاملة (من المتصفح أو الـ Terminal).
