# دليل المرحلة 6️⃣ - اختبار الأمان والحماية

⚠️ استبدل ملفات المشروع، ثم:
```
npm install
npm run dev
```
(لتثبيت مكتبتي `otplib` و `qrcode` الجديدتين لخاصية 2FA)

---

## 📁 الملفات الجديدة/المعدّلة

```
src/
├── middleware.ts              ← هيدرز أمان HTTP على كل الطلبات (جديد)
├── lib/
│   ├── sanitize.ts             ← حماية من NoSQL Injection + ReDoS (جديد)
│   └── rateLimit.ts            ← حماية من محاولات الدخول المتكررة (جديد)
├── models/User.ts (محدّث)      ← حقول 2FA وقفل الحساب
├── app/
│   ├── login/page.tsx (محدّث)  ← يدعم إدخال كود 2FA
│   ├── api/auth/
│   │   ├── login/route.ts (محدّث) ← Rate limiting + قفل الحساب + دعم 2FA
│   │   ├── register/route.ts (محدّث) ← تنظيف المدخلات
│   │   └── 2fa/
│   │       ├── setup/route.ts    ← توليد رمز QR
│   │       ├── verify/route.ts   ← تأكيد وتفعيل 2FA
│   │       └── disable/route.ts  ← تعطيل 2FA
│   ├── api/products/route.ts (محدّث) ← حماية من ReDoS في البحث
│   └── admin/security/page.tsx ← واجهة تفعيل/تعطيل 2FA
```

---

## ✅ اختبارات الأمان (بالترتيب)

### 1. اختبار الحماية من محاولات الدخول المتكررة (Rate Limiting)
في Postman أو المتصفح، حاول تسجيل الدخول بكلمة مرور خاطئة **6 مرات متتالية بسرعة** بنفس البريد الإلكتروني.

**يجب أن ترى**: في المحاولة السادسة، رسالة خطأ مختلفة: "محاولات كثيرة جداً. حاول مرة أخرى بعد X دقيقة" (كود الحالة 429).

### 2. اختبار قفل الحساب
لاحظ أنه بعد 5 محاولات خاطئة على **نفس الحساب**، حتى لو غيّرت الـ IP نظرياً، الحساب نفسه يُقفل لمدة 15 دقيقة (كود الحالة 423). جرّب الدخول بكلمة المرور **الصحيحة** الآن - يجب أن يُرفض أيضاً لأن الحساب مقفل مؤقتاً.

### 3. اختبار حقن NoSQL Injection
في Postman، جرّب إرسال هذا الجسم لتسجيل الدخول عمداً:
```json
{
  "email": { "$ne": "" },
  "password": { "$ne": "" }
}
```
**يجب أن ترى**: خطأ "بيانات غير صحيحة" من Zod (لأن `email` يجب أن يكون نصاً وليس كائناً) - هذا يثبت أن الحماية تعمل ولم يُسمح لهذا الحقن بتجاوز تسجيل الدخول.

### 4. اختبار هيدرز الأمان
افتح المتصفح، اذهب لأي صفحة في الموقع، افتح أدوات المطور (F12) → تبويب Network → اضغط على أي طلب → تبويب Headers.
**يجب أن ترى** هيدرز مثل: `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`.

### 5. تفعيل المصادقة الثنائية (2FA) للأدمن
1. سجّل دخول كأدمن، اضغط "الأمان" في القائمة الجانبية للوحة التحكم (تمت إضافتها الآن - كانت الصفحة موجودة لكن غير مرتبطة بالقائمة)، أو اذهب مباشرة لـ `/admin/security`
2. اضغط "تفعيل المصادقة الثنائية"
3. حمّل تطبيق **Google Authenticator** (أو Microsoft Authenticator) على هاتفك
4. امسح رمز QR الظاهر
5. أدخل الكود المكوّن من 6 أرقام الذي يظهر في التطبيق، اضغط "تأكيد"
6. **يجب أن ترى**: رسالة "تم تفعيل المصادقة الثنائية بنجاح"

### 6. اختبار الدخول مع 2FA مفعّل
1. سجّل خروج
2. سجّل دخول بحساب الأدمن (بريد + كلمة مرور) كالمعتاد
3. **يجب أن ترى**: حقل جديد يظهر يطلب "كود المصادقة الثنائية"
4. افتح تطبيق Google Authenticator، انسخ الكود الحالي (يتغيّر كل 30 ثانية!)، أدخله بسرعة، اضغط "تأكيد الدخول"
5. **يجب أن تدخل بنجاح**

### 7. تعطيل 2FA (اختياري)
من `/admin/security`، أدخل كلمة المرور واضغط "تعطيل" - تأكد أن تسجيل الدخول التالي لا يطلب الكود.

---

## 🎯 ماذا بعد؟
هذه آخر مرحلة تطوير! المرحلة 7 الأخيرة هي: **النشر الفعلي** - رفع المتجر على الإنترنت (Vercel أو AWS)، ربط دومين حقيقي، شهادة SSL/HTTPS حقيقية، وإضافة بيانات حقيقية (أقسام ومنتجات فعلية) ليصبح المتجر جاهزاً للعمل الفعلي.

## ❌ إذا واجهت خطأ
أرسل لي: اسم الاختبار (1-7) + رسالة الخطأ الكاملة.
